# Integration tests

This suite is reserved for root-level cross-package integration tests. Current API/database integration coverage lives under `apps/api/tests`; add executable tests here when a shared cross-workspace boundary is introduced.
