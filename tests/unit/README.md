# Unit tests

This suite is reserved for isolated root-level domain and UI contract tests. Current executable Python unit tests live under `apps/api/tests`; add root tests here when shared Python logic is introduced.
